# ── H2 In-Memory Database (default – no external server needed) ──────────────
hibernate.connection.driver_class=org.h2.Driver
hibernate.connection.url=jdbc:h2:mem:bureauetudes;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
hibernate.connection.username=sa
hibernate.connection.password=
hibernate.dialect=org.hibernate.dialect.H2Dialect

# ── MySQL (comment the H2 block above and uncomment below) ───────────────────
#hibernate.connection.driver_class=com.mysql.cj.jdbc.Driver
#hibernate.connection.url=jdbc:mysql://localhost:3306/bureau_etudes?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
#hibernate.connection.username=root
#hibernate.connection.password=
#hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ── Common settings ───────────────────────────────────────────────────────────
hibernate.hbm2ddl.auto=create-drop
hibernate.show_sql=true
hibernate.format_sql=false
hibernate.connection.pool_size=5
